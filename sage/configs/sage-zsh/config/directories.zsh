# Changing/making/removing directory
# Sage note: Don't actually think I use any of these
setopt auto_cd
setopt auto_pushd
setopt pushd_ignore_dups
setopt pushdminus

# List directory contents
alias lsa='ls -lah'
alias l='ls -lah'
alias ll='ls -lh'
alias la='ls -lAh'
